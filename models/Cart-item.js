class CartItem {
	constructor(qty, productPrice, ProductTitle, sum) {
		this.qty = qty;
		this.productPrice = productPrice;
		this.ProductTitle = ProductTitle;
		this.sum = sum;
	}
}

export default CartItem;
